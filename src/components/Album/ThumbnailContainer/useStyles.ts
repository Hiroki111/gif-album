import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  container: {
    marginTop: '32px',
    marginBottom: '32px',
  },
  loading: {
    textAlign: 'center',
  },
  endMessage: {
    textAlign: 'center',
  },
}));
